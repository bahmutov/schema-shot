'use strict'

/* global describe, it */
describe('schema-shot', () => {
  const schemaShot = require('.')
  it('write this test', () => {
    console.assert(schemaShot, 'should export something')
  })
})
